package com.bigoloo.finance.home.persistance

/*
import org.junit.Assert
import org.junit.Test
import org.junit.runner.RunWith

*/
/**
 * Created by Amin Bahiraei (mr.bahiraee@gmail.com) at  1/18/18.
 *//*



@RunWith(AndroidJUnit4::class)
class TableDatabaseTest : DatabaseTest() {

    @Test
    fun test_home_insertion_and_deletion() {
*/
/*        val home = HomeDAO(timestamp = 123,
                location = LocationDAO(latitude = 1.0, longitude = 1.0), name = "SDFSDFSDFDS")
        home.save().subscribe()
        val driverLocations = driverLocationDAOList()
        //check list size
        Assert.assertEquals(1, driverLocations.size)
        //check added DriverLocation
        driverLocations.first().apply {
            Assert.assertEquals(123, timestamp)
            Assert.assertEquals(1f, accuracy)
            Assert.assertEquals(1f, bearing)
            Assert.assertEquals(1f, speed)
            location?.apply {
                Assert.assertEquals(1.0, latitude)
                Assert.assertEquals(1.0, longitude)
            }
        }
        //check deletion
        home.delete().subscribe()
        MoreAsserts.assertEmpty(driverLocationDAOList())*//*

        Assert.assertTrue(true)
    }

    //  private fun driverLocationDAOList() = (select from D::class).list
}
*/
